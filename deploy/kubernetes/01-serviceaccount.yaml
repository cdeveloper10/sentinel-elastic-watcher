apiVersion: v1
kind: ServiceAccount
metadata:
  name: sentinel
  namespace: sentinel

# No token is mounted. Neither host talks to the Kubernetes API, and a projected token in a pod that does
# not need one is a credential sitting where an exploited process would find it.
automountServiceAccountToken: false
