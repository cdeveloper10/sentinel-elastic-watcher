<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Sentinel.Samples.ServiceStub</RootNamespace>

    <!-- A sample, referenced by nothing. Kept out of any package or publish output. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

</Project>
